<% if user_signed_in? %>
  <% csrf_token = form_authenticity_token %>
  <%= react_component("App", { currentUser: current_user.slice(:id, :email, :current_type), token: csrf_token }) %>
<% else %>
  <%= react_component("App") %>
<% end %>
